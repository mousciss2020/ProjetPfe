
#server.port=8081
#spring.application.name = Gestion_Employes
#server.port est utilisé pour définir
#le port d’écoute du serveur Apache Tomcat intégré à Spring Boot.

#server.servlet.context-path=/GestionEmployes
# ===============================
# = DATA SOURCE
# ===============================

# Set here configurations for the database connection

# Connection url for the database "netgloo_blog"
spring.datasource.url = jdbc:mysql://localhost:3306/dbtreroc?useSSL=false&serverTimezone=UTC

# Username and password
spring.datasource.username = root
spring.datasource.password = 

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
spring.servlet.multipart.max-file-size=200MB
spring.servlet.multipart.max-request-size=500MB

spring.mvc.contentnegotiation.favor-parameter: true
spring.mvc.contentnegotiation.media-types.pdf: application/pdf
spring.mvc.contentnegotiation.media-types.xlsx: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
spring.mvc.contentnegotiation.media-types.csv: text/csv
spring.mvc.contentnegotiation.media-types.xml: application/xml
spring.mvc.contentnegotiation.media-types.json: application/json
